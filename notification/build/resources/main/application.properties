spring.application.name=notification
server.port=${PORT:8083}

# Email (Gmail SMTP)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:juanbobadilla0104@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:wwit ooeo smwh licq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.notification.email.from=${APP_NOTIFICATION_EMAIL_FROM:juanbobadilla0104@gmail.com}
app.notification.email.enabled=${APP_NOTIFICATION_EMAIL_ENABLED:true}
